{$RELAY_DOMAIN} {
	encode zstd gzip

	# WebSocket der Desktop-App und Webhook-Endpunkt liegen hinter demselben Host.
	reverse_proxy relay:8080

	log {
		output file /data/access.log
		format json
	}
}
